# Runtime dependencies for the Streamlit dashboard (app.py + modules/).
# Pinned to the versions verified locally on Python 3.11 so the deploy is reproducible.
# Deploy target: Streamlit Community Cloud — select Python 3.11 in "Advanced settings".
streamlit==1.61.1
pandas==3.0.3
numpy==2.1.2
plotly==6.9.0
# Reads the packaged parquet snapshot in data/packaged/ (primary data path).
pyarrow==20.0.0
# Fallback path only: rebuild the model from data/Business Case.xlsx if the
# snapshot is missing. Tiny package, keeps the app self-healing.
openpyxl==3.1.5
