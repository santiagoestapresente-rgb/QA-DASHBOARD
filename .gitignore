__pycache__/
*.pyc
.streamlit/secrets.toml
.env
.venv/
venv/

# Local build cache (regenerable, ~34 MB). The versioned data lives in data/packaged/.
data/cache/

# Personal dashboard label/theme tweaks (sidebar Edit labels → Save)
ui_overrides.json

# Office lock files
~$*
